import type { Metadata } from 'next';
export const metadata:Metadata={title:'My Nexavoris Dashboard',robots:{index:false,follow:false,nocache:true},referrer:'no-referrer'};
export default function AccountLayout({children}:{children:React.ReactNode}){return children}
