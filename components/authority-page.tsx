import { ArrowRight, CheckCircle2 } from 'lucide-react';
export type AuthoritySection={title:string;body:string;items?:string[]};
export function AuthorityPage({eyebrow,title,intro,sections,cta=true}:{eyebrow:string;title:string;intro:string;sections:AuthoritySection[];cta?:boolean}){return <main className="authority-page"><header className="authority-hero section-shell"><span className="section-index">{eyebrow}</span><h1>{title}</h1><p>{intro}</p></header><div className="authority-body section-shell">{sections.map((section,index)=><section key={section.title}><span>{String(index+1).padStart(2,'0')}</span><div><h2>{section.title}</h2><p>{section.body}</p>{section.items&&<ul>{section.items.map(item=><li key={item}><CheckCircle2 size={17}/>{item}</li>)}</ul>}</div></section>)}</div>{cta&&<footer className="authority-cta section-shell"><div><span className="section-index">DISCOVERY</span><h2>Start with the operation you have today.</h2><p>We review current systems, workflows, priorities, and constraints before recommending a next step.</p></div><a className="button primary" href="/contact">Request a consultation <ArrowRight size={17}/></a></footer>}</main>}
