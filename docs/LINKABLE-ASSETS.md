# Useful tools and original assets

## Existing tools to strengthen

The free readiness assessment and ROI methodology already exist. Retain methodology links, inspectable inputs, conditional restaurant questions and honest limitations. Existing restaurant scenario and SQL tests were run. Do not create separate AI/ERP assessments with duplicative scores merely for keywords.

## Priorities

1. Restaurant POS total-cost worksheet: use verified hardware/seat terms, tax and subscription assumptions, clearly separate one-time and recurring costs, allow real inputs and date-stamp offers. Publish only after owner confirms current commercial terms.
2. Odoo implementation scope estimator: show ranges based on documented migration, integrations, customization, training and support assumptions. Requires approved rate/effort evidence; do not invent a precise quote.
3. Private AI sizing worksheet: inputs for model, quantization, context, concurrency, memory overhead and measured target workload. Needs reproducible hardware benchmarks and actual model versions before numeric recommendations.
4. ERP ROI worksheet: separate observed baseline, assumptions, incremental recurring costs and sensitivity. Present scenarios, not promised savings.
5. Open-source permission/grounding evaluation checklist: use the new architecture failure cases as a source-backed starting point, with no customer data or keys. A tested reusable harness is more useful than an arbitrary score.

Original assets now published: readable read-path/write-path diagrams and acceptance tables on existing guide/service pages and the architecture reference. These render without JavaScript. Future demos must show actual systems and sanitized data; obtain consent before using customer screenshots. No fake calculator or video schema was added.
